# Ants Simulator backend

This is a first attempt at a ant simulation backend.
It uses `postgres` together with `nodejs`.
The issue with this approach is that it is way to slow.
So I switched to rust.

## Start service

```bash
npm run dev
```
